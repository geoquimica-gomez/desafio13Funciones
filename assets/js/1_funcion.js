// 1. Transforma esta declaración de función a una función de expresión. El ejercicio debe quedar en un archivo nombrado `1_funcion.js` (1 Punto)

function example(a, b, c){
return a+b+c
}

// Función de expresión

const example1 = function (a, b, c) {
    return a+b+c
}
